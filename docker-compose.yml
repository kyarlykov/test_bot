version: "3"
services:
  bot:
    build:
      context: ./Dockerfile/
    image: bot
    container_name:
      projectbot
    command:
      python test_bot.py
    env_file:
      - .env
    restart:
      always
